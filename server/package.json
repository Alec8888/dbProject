{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "oracledb": "^6.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "scripts": {
    "server": "node server.js",
    "client": "npm run dev --prefix ../client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  }

   
  
}
